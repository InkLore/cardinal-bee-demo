import { BaseResource } from "../resources/base";
import { Alkane } from "../schemas/alkane";
export declare class AlkaneResource extends BaseResource {
    list({ sort, type, page, }?: {
        sort?: "newest" | "oldest";
        type?: "TOKEN" | "CONTRACT" | "NFT_COLLECTION" | "NFT_ITEM";
        page?: number;
    }): Promise<Alkane[]>;
    getInfo({ id }: {
        id: string;
    }): Promise<{
        symbol: string | null;
        id: string;
        name: string | null;
        type: "TOKEN" | "CONTRACT" | "NFT_COLLECTION" | "NFT_ITEM";
        logo_url: string | null;
        premined_supply: string;
        amount_per_mint: string | null;
        mint_count_cap: string | null;
        deploy_txid: string | null;
        deploy_timestamp: string | null;
        current_supply: string;
        current_mint_count: number;
    }>;
    getMeta({ id }: {
        id: string;
    }): Promise<{
        name: string | null;
        methods: {
            name: string;
            params: {
                name: string;
                type: string;
            }[];
            opcode: number;
            returns: string;
        }[] | null;
    }>;
}
//# sourceMappingURL=alkane.d.ts.map