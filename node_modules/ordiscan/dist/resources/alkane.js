"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AlkaneResource = void 0;
const base_1 = require("../resources/base");
class AlkaneResource extends base_1.BaseResource {
    async list({ sort, type, page, } = {}) {
        const params = new URLSearchParams();
        let url = "/alkanes";
        if (sort) {
            params.append("sort", sort);
        }
        if (type) {
            params.append("type", type);
        }
        if (page) {
            params.append("page", page.toString());
        }
        if (params.size) {
            url += `?${params.toString()}`;
        }
        return this.client.fetch(url);
    }
    async getInfo({ id }) {
        return this.client.fetch(`/alkane/${id}`);
    }
    async getMeta({ id }) {
        return this.client.fetch(`/alkane/${id}/meta`);
    }
}
exports.AlkaneResource = AlkaneResource;
//# sourceMappingURL=alkane.js.map